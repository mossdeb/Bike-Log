/**
 * The two addresses Bikit publishes.
 *
 * Split by what the reader wants, not by who reads it: one for something that
 * is broken, one for everything else. They live here rather than next to each
 * page that prints them — the legal documents name the first as the contact
 * for data-protection requests, and the support page names both, and an
 * address that appears in a policy must not drift from the one on the page
 * that invites people to write.
 */
export const SUPPORT_EMAIL = "support@bikit.app";
export const CONTACT_EMAIL = "hello@bikit.app";
